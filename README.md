# Data Classifier

A Python script that reads structured data and applies classification rules to reorganise it. Useful for preparing messy datasets before analysis.

The script takes an input file, applies a set of rules, and outputs a cleaned, categorised version.

## Usage

```bash
python classifier.py
```
